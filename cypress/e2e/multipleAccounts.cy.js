describe("Login with different accounts and do daily check in", () => {
  before(() => {
    cy.clearLocalStorage();
    cy.clearCookies();
    cy.visit(Cypress.env("baseUrlWeb"));
  });
  it("", () => {});
});
